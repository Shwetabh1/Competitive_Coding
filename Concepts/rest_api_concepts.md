REST - Representational State Transfer

It is an architectural style where every content is treated as resource. These resources can be Text Files, HTML pages.
Representations are transferred between clients and servers.

It is stateless built on HTTP which is stateless.

Uniform Resource
Cacheable
Code on Demand

Representation = Resource Mutated

Stateful App is one that stores information about what has happened or changed since it started running.

Stateless gives same response to the same request function everytime.

API Design interview questions - check dev.to saved posts.

